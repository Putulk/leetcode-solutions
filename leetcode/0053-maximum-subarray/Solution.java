class Solution {
    public int maxSubArray(int[] nums) {
        int n = nums.length;
        int maxSum = 0;
        int maxSum

        for(int i=0; i<n; i++){
            sum = sum + nums[i];
            if(sum > )
        }
        return maxSum;
    }
}