class Solution {
    public int search(int[] nums, int target) {
        int n = nums.length;

        for(int i=0; i<n; i++){
            int left = 0;
            int right = n-1;
            int mid = left+(right-left)/2;

            if(n)
        }
    }
}