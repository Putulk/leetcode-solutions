class Solution {
    public int subarraySum(int[] nums, int k) {
        int n = nums.length;
        int cnt = 0;
        Map<Integer, Integer> map = new HashMap<>();
        for(int i=0; i<n; i++){
            int sum = 0;
            sum = nums[i];
            if(sum == )
        }
        return cnt;
    }
}