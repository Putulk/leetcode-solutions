class Solution {
    public boolean containsDuplicate(int[] nums) {
        int n = nums.length;
        Set<Intger> set = new HashSet<>();
        for(int i=0; i<n; i++){
            if(set.con)
        }
    }
}