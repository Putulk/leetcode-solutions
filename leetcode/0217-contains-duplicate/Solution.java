class Solution {
    public boolean containsDuplicate(int[] nums) {
        int n = nums.length;
        Arrays.sort(nums);
        for(int i=; i<n; i++){
            iif(nums[i] == nums[i-1]){

            }
        }
        return false;
    }
}