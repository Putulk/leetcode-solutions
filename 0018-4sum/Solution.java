class Solution {
    public List<List<Integer>> fourSum(int[] nums, int target) {
        int n = nums.length;

        for(int i=0; i<n-1; i++){
            int j = i+1;
            int k = n-1;
            while(j < k){
                int comp = target - (nums[i]+nums[j]nums[k]);
            }

        }
    }
}